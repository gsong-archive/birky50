version: '3.5'

services:
  # Primary services
  app:
    build:
      context: .
    image: birky50/app
    container_name: birky50_app
    command: npm start
    volumes:
      - .:/app
    ports:
      - 3000:3000
